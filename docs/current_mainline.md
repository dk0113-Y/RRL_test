# Current Mainline

The current formal mainline is the shared-semantic dual-state architecture.

## Active Mainline

- `SharedSemanticLayer`
- Advantage state built from the local decision canvas
- Value state built from accessible unknown blocks and entry clusters
- Semantic dueling head on top of that split state

## Historical Only

The old near/mid/token framing remains in the codebase only as historical reference or as lower-level implementation residue. It is not the formal training path and must not be used to redefine current round semantics.

## Current Baseline Anchor

- The source repo marks `4.9_30万轮基线` as the current default main baseline identifier by bootstrap inference from local run inventory.
- That identifier is a long-term anchor, but individual later runs may still require subgroup-specific comparability checks before they can be compared directly.
