# Automation Scope

Currently, the automation focuses on round-based decision making, metrics evaluation, and policy refinement. The system avoids long-lived sessions and instead relies on the structured files in this repository as the single source of truth for context.